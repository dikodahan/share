{
  "name": "dikoplus",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean:backend": "npm run clean --prefix ./backend",
    "clean:frontend": "npm run clean --prefix ./frontend",
    "clean:root": "rm -rf lib node_modules",
    "clean": "npm run clean:backend && npm run clean:frontend && npm run clean:root",
    "watch": "tsc -w",
    "build": "./build.sh",
    "prebuild": "./install.sh",
    "start": "node lib/backend/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "devDependencies": {
    "@types/express": "^4.17.15",
    "typescript": "^4.9.4"
  }
}